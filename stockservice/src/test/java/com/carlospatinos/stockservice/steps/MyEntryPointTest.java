package com.carlospatinos.stockservice.steps;

// @Suite
// @SelectClasspathResource("features")
// @ConfigurationParameter(key = Constants.GLUE_PROPERTY_NAME, value = "com.carlospatinos.stockservice.steps")
public class MyEntryPointTest {
    // Can be left empty unless otherwise needed
}
